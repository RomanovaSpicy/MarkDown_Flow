<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_dialog_help"
    android:padding="@dimen/dialog_padding"
    android:clipToOutline="true">

    <!-- Title -->
    <TextView
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/help_title"
        android:textAppearance="@style/TextAppearance.App.Title"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ScrollView for Help Text -->
    <ScrollView
        android:id="@+id/scroll_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_m"
        android:maxHeight="400dp"
        app:layout_constraintTop_toBottomOf="@id/title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/help_text"
            android:textAppearance="@style/TextAppearance.App.Body"
            android:lineSpacingMultiplier="1.5"/>
    </ScrollView>

    <!-- Copy Symbol Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/copy_symbol_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:minHeight="@dimen/button_height_small"
        android:layout_marginTop="@dimen/space_l"
        android:layout_marginEnd="@dimen/space_s"
        android:text="@string/help_copy_symbol"
        android:textColor="@color/warning"
        android:background="@drawable/bg_button_help"
        android:stateListAnimator="@null"
        app:layout_constraintTop_toBottomOf="@id/scroll_view"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/close_button"
        app:backgroundTint="@null"/>

    <!-- Close Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/close_button"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="@string/help_close"
        android:textColor="@color/text_secondary"
        android:background="@drawable/bg_button_secondary"
        android:stateListAnimator="@null"
        app:layout_constraintTop_toTopOf="@id/copy_symbol_button"
        app:layout_constraintBottom_toBottomOf="@id/copy_symbol_button"
        app:layout_constraintStart_toEndOf="@id/copy_symbol_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:backgroundTint="@null"/>

</androidx.constraintlayout.widget.ConstraintLayout>
